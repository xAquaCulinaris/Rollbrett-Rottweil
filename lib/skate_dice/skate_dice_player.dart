import 'package:flutter/material.dart';

class SkateDicePlayer extends StatelessWidget {
  const SkateDicePlayer({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
